import Editor from "@/components/editor/editor";
import React from "react";

const Page = () => {
  return (
    <div className="flex flex-col items-center justify-center">
      <Editor image="" logo="" onSave={() => {}} />
    </div>
  );
};

export default Page;
