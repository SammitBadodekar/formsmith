/**
 * Default form customization values
 * Re-exported from @/lib/shared package
 */
export { defaultFormCustomizations } from "@/lib/shared";

/**
 * Default form data structure
 */
export const defaultFormData = [];

/**
 * Default workspace settings
 */
export const defaultWorkspaceSettings = {
  theme: "light",
  notifications: true,
};
